import random
import math
import datetime
import argparse



# ajouter, modifier, supprimer, rechercher des contacts, et lister tous les contacts avec pagination.

def contact(name):  #name, number phone

    for i in name:
        nom, number = i

        return f"{nom}:{number}"
    
def ajouter():